# Мониторинг подписки ПРАЙМ



# Мониторинг подписки ПРАЙМ

Учебный проект курса «Программирование на Python для бизнес-аналитики»
(ВШБ НИУ ВШЭ, БАЦРФ, 2026/2027).

**Автор:** Троценко Анастасия, БАЦРФ 262

---

## Что здесь

Аналитический сервис и блокноты для мониторинга обращений в службу поддержки подписки ПРАЙМ (таблица `support_tickets`, сегмент «кэшбэк»). 

Проект агрегирует данные из базы, выполняет очистку «грязных» входящих записей, рассчитывает операционную нагрузку на специалистов поддержки и отслеживает устойчивость метрик во времени при помощи статистического анализа выборок.

---

## Как запустить

Нужны [Git](https://git-scm.com/) и [uv](https://docs.astral.sh/uv/).
Python отдельно ставить не нужно — его поставит uv.

```bash
git clone git@github.com:NastyaTrotsenko/prime-monitor.git
cd prime-monitor
uv sync
```

Доступ к учебной базе выдаёт бот курса по команде `/db`. Полученную строку
подключения кладём в `.env`:

```bash
cp .env.example .env
# и вписать в .env строку из бота
```

Проверка, что всё встало:

```bash
uv run python -c "from prime.config import get_engine; print(get_engine().connect().execute(__import__('sqlalchemy').text('select current_user')).scalar())"
```

Запуск JupyterLab:

```bash
uv run jupyter lab
```

---

## Структура

| Путь | Что это |
|---|---|
| `src/prime/` | код проекта: подключение, метрики |
| `notebooks/` | ноутбуки: разведка, расчёты, графики |
| `pyproject.toml` | какие библиотеки нужны проекту |
| `uv.lock` | какие версии встали на самом деле |
| `.env.example` | образец файла с доступами; настоящий `.env` в репозиторий не идёт |

---

## Что здесь считается

<!--
  Доля успешных обращений (ok_share): отношение количества обращений со статусом ok = True ко всем обращениям сегмента.
Длительность решения (val_per_ok_row): среднее время работы операторов над успешным тикетом (в часах value).
Нагрузка на участника (val_per_actor): суммарное время решения проблем на одного уникального клиента.
Взвешенная нагрузка: сумма затраченных часов с поправкой на коэффициенты активности в разные дни недели (WEIGHTS).
Стабильность выборки: стандартное отклонение и ошибка среднего при многократном случайном семплировании с добавлением соли в хеш actor_id.
-->

---

## Использование ИИ

См. [ДЕКЛАРАЦИЯ-ИИ.md](ДЕКЛАРАЦИЯ-ИИ.md).
